NODE_ENV=development
PORT=3000

# Database (used by pg)
# - If you're running the API locally: use localhost
# - If you're running via docker-compose: docker-compose overrides this to use the "db" host
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/api_analytics

# JWT
JWT_SECRET=dev_only_change_me
